#!/bin/bash
docker login -u smritiorganization  -p pass123@123

# Pull the Docker image from the registry
docker pull smritiorganization/ubuntu/apache2

# Run the Docker container
docker run -d -p 8080:8080 smritiorganization/ubuntu/apache2
